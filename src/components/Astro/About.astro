---
/**
 * ABOUT COMPONENT - REZCODE (Clean Business Style)
 * Fokus pada kejelasan visi dan misi tanpa elemen tambahan yang tidak perlu.
 */
import aboutData from "../../data/about.json";
import { CheckCircle2 } from "lucide-react";
---

<section id="about" class="py-20 md:py-32 bg-[#020406] overflow-hidden">
  <div class="max-w-6xl mx-auto px-6">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 md:gap-20 items-start">
      
      <div data-aos="fade-up">
        <span class="text-brand-primary font-mono text-xs font-bold uppercase tracking-[0.4em] mb-4 block">
          About RezCode
        </span>
        <h2 class="text-4xl md:text-6xl font-extrabold text-white leading-tight tracking-tighter mb-8">
          Beyond Code.<br />
          <span class="text-transparent bg-clip-text bg-linear-to-r from-brand-primary to-cyan-400">Beyond Limits.</span>
        </h2>
        <p class="text-lg text-slate-400 leading-relaxed max-w-xl">
          RezCode is a premium digital agency dedicated to transforming complex challenges into seamless, high-performance web experiences. We engineer digital solutions that are secure, scalable, and built for the future.
        </p>
      </div>

      <div class="space-y-8" data-aos="fade-up" data-aos-delay="200">
        <div class="group">
          <h3 class="text-white text-xs font-black uppercase tracking-[0.3em] mb-4 flex items-center gap-2">
            <span class="w-8 h-px bg-brand-primary/40 group-hover:w-12 transition-all"></span> Our Vision
          </h3>
          <p class="text-slate-300 text-base md:text-lg leading-relaxed border-l border-white/10 pl-6 ml-4">
            To build the <span class="text-white font-bold">fastest</span>, most elegant, and <span class="text-white font-bold">secure</span> digital solutions that empower brands to scale without boundaries.
          </p>
        </div>

        <div class="group pt-4">
          <h3 class="text-white text-xs font-black uppercase tracking-[0.3em] mb-6 flex items-center gap-2">
            <span class="w-8 h-px bg-brand-primary/40 group-hover:w-12 transition-all"></span> Strategic Mission
          </h3>
          <ul class="grid grid-cols-1 sm:grid-cols-2 gap-4 ml-4">
            {
              aboutData.map((item) => (
                <li class="flex items-center gap-3 p-2 rounded-lg transition-colors group/item">
                  <CheckCircle2 size={16} className="text-brand-primary opacity-40 group-hover/item:opacity-100 transition-opacity" />
                  <span class="text-slate-400 group-hover/item:text-white transition-colors text-sm font-medium">
                    {item.text}
                  </span>
                </li>
              ))
            }
          </ul>
        </div>
      </div>
      
    </div>
  </div>
</section>

<style>
  /* Standar AOS simple untuk performa */
  @media (max-width: 768px) {
    [data-aos] {
      transform: none !important;
      transition-duration: 400ms !important;
    }
  }
</style>